{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open nalam.me (Browser-Sync)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "dev: Browser-Sync"
    },
    {
      "name": "Attach: already running dev server",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
